<Window x:Class="DnD_CharSheet_5e.BuyOrAddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DnD_CharSheet_5e"
        mc:Ignorable="d"
        Title="Buy or Add Item" Height="200" Width="400" Closing="Window_Closing">

    <StackPanel>
        
        <Grid Margin="0 50 0 40">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Button x:Name="BuyItemBtn" Content="Buy Item" Click="BuyItemBtn_Click" Width="150" Style="{StaticResource EditBtn}" Grid.Column="0"/>
            <Button x:Name="JustAddBtn" Content="Just Add Item" Click="JustAddBtn_Click" Width="150" Style="{StaticResource EditBtn}" Grid.Column="1"/>

        </Grid>

        <Button x:Name="CancelBtn" Content="Cancel" Click="CancelBtn_Click" Width="150" Style="{StaticResource EditBtn}" Grid.Column="0"/>

    </StackPanel>
    
</Window>
